export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned
}
export enum TokenTypes {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  VerifyEmailToken
}
export enum MediaTypes {
  Image,
  Video,
  HLS
}
export enum EncodingStatus {
  Pending,
  Processing,
  Success,
  Failed
}
